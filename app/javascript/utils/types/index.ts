export type NavigationItemType = {
  label: string;
  key: string;
  url?: string;
};

export type LoginItemType = {
  username: string;
  password: string;
  rememberme: string;
};

export type ButtonType = {
  name: string;
  label: string;
};
